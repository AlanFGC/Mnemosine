import { EventType } from "./pluginInfo";
function OnClickListener(): (event: EventType) => void {
  const OnClickListener(event: EventType) =>{
    const context = LexicalComposer
  }
  return OnClickListener 
};

export default OnClickListener;